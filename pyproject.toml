[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "air-quality-forecast"
version = "0.1.0"
description = "Air quality forecasting project"
authors = [{name = "Federico"}]
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]